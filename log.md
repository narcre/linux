## journalctl
```
sudo journalctl -u kubelet -f
```

## shoutdown & reset
```
who -b


last reboot | less

last reboot | head -1

last -x|grep shutdown | head -1

last
last -x
last -x reboot
last -x shutdown


uptime -s

last reboot
```
